Simply install `express` using `npm` and start the server as `node index.js`

It uses GET request which is what web browsers does, simply convert your mailto as:
```
http://localhost:3000/mailto?email=zavocc1030@outlook.com?subject=Hello world
```
It must be under `/mailto` GET request and must have `email=` parameter in your address bar to get started